{
  "name": "electron-bridge",
  "version": "1.0.0",
  "description": "Safely expose main process modules of Electron to renderer process through IPC.",
  "author": "Louis Poirier",
  "license": "MIT",
  "private": true,
  "bugs": {
    "url": "https://github.com/poirierlouis/electron-bridge/issues"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/poirierlouis/electron-bridge.git"
  },
  "keywords": [
    "electron",
    "ipc",
    "renderer",
    "main",
    "process",
    "bridge",
    "context",
    "isolation",
    "sandbox",
    "secure"
  ],
  "main": "index.js",
  "types": "index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "shx rm -rf dist/",
    "generate": "eb generate bridge.config.json",
    "build": "npm run clean && webpack --config webpack.dev.js",
    "build:prod": "npm run clean && webpack --config webpack.prod.js",
    "pack": "node pack.js && npm pack ./dist"
  },
  "devDependencies": {
    "electron": ">=9.0.0",
    "shx": "^0.3.3",
    "ts-loader": "^9.2.5",
    "typescript": "^4.4.2",
    "webpack-cli": "^4.8.0",
    "electron-bridge-cli": "file:./cli/electron-bridge-cli-1.0.0.tgz"
  }
}
